---
description: Run sequential /xia rounds against a ranked list of repos, using the codebase as the running distillation.
---

Invoke: `Skill(skill="xia", args="group $ARGUMENTS")` — pass a newline-separated list of repo URLs as args.
